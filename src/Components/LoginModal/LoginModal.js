import React from 'react';
import Modal from '../../hoc/Modal/Modal';

const LoginModal = () => {
    return (
        <div>
            <Modal> Testing Modal</Modal>
        </div>
    );
};

export default LoginModal;